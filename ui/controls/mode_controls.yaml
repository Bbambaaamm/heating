# =====================================================================
# Soubor   : config/heating/ui/controls/mode_controls.yaml
# Modul    : UI – Režimy systému + Boost
# Účel     : Ovládací prvky pro uživatelské přepínání režimů topení
# Autor    : Michal
# Poslední úprava : 2025-11-19
# =====================================================================
# 📌 Obsah:
#   input_select.topny_rezim
#   input_boolean.boost_now
#   input_number.boost_minutes
#   input_datetime.boost_until
#   input_boolean.topny_system_enable
#   input_boolean.kotel_ignore_open_windows
#   input_number.eco_temp_default
#
# 📝 Poznámky:
# - Režimy Auto / Eco / Boost / Off ovlivňují prahy v policy_effective.yaml
# - Boost se aktivuje input_boolean.boost_now → autom. nastaví boost_until
# - input_boolean.topny_system_enable slouží jako „master power“
# =====================================================================


# =====================================================================
# 🔽 Výběr globálního režimu topení
# =====================================================================
input_select:
  topny_rezim:
    name: "Topení – režim"
    icon: mdi:home-thermometer-outline
    initial: Auto
    options:
      - Auto     # standardní režim
      - Eco      # úsporný režim (vyšší prahy, nižší komfort)
      - Boost    # krátkodobé intenzivní topení
      - "Off"      # úplné vypnutí systému


# =====================================================================
# 🔘 Přepínače (boolean helpers)
# =====================================================================
input_boolean:

  # ▶️ Ruční aktivace BOOStu
  boost_now:
    name: "Boost – zapnout nyní"
    icon: mdi:fire

  # ▶️ Hlavní povolení topného systému
  topny_system_enable:
    name: "Topení – povoleno"
    icon: mdi:power
    initial: on

  # ▶️ Volba ignorovat otevřená okna (globální override)
  kotel_ignore_open_windows:
    name: "Kotel – ignorovat otevřená okna"
    icon: mdi:window-open-variant
    initial: true


# =====================================================================
# 🔢 Číselné hodnoty (minuty Boostu, ECO teplota…)
# =====================================================================
input_number:

  # ⏱️ Délka Boostu v minutách
  boost_minutes:
    name: "Boost – délka (min)"
    min: 10
    max: 180
    step: 10
    unit_of_measurement: min
    icon: mdi:timer
    initial: 60

  # 🌿 ECO cílová teplota
  eco_temp_default:
    name: "ECO teplota (°C)"
    min: 10
    max: 22
    step: 0.5
    unit_of_measurement: "°C"
    icon: mdi:leaf
    initial: 15.0


# =====================================================================
# 🗓️ Pomocník – čas ukončení Boostu
# =====================================================================
input_datetime:
  boost_until:
    name: "Boost – platí do"
    has_date: true
    has_time: true
