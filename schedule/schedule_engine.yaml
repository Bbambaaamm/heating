# =====================================================================
# Soubor   : config/heating/schedule/schedule_engine.yaml
# Modul    : Schedule Engine – sdílená logika rozvrhů (více zón)
# Účel     : Jednotná logika pro nastavování komfortních a útlumových teplot
# Autor    : Michal
# Poslední úprava : 2025-11-19
# =====================================================================
# 📌 Přehled:
# - senzory:
#     • sensor.heating_setback_temp_c         → globální útlumová teplota
# - skripty:
#     • script.schedule_set_zone_comfort      → nastaví komfort pro zónu
#     • script.schedule_set_zone_setback      → nastaví útlum pro zónu
#
# Použití:
# - volají je start/end automatizace rozvrhů jednotlivých zón
# - režim Auto/Eco/Boost/Off se promítá do výpočtu cílové teploty
# =====================================================================

template:
  - sensor:
      # ----------------------------------------------------------------
      # Globální útlumová teplota (setback) – společná pro všechny zóny
      # ----------------------------------------------------------------
      - name: "heating_setback_temp_c"
        unique_id: heating_setback_temp_c
        unit_of_measurement: "°C"
        state: "17"  # případnou změnu lze později parametrizovat helperem

# =====================================================================
# Skripty – sdílená logika pro KOMFORT / ECO / ÚTLUM
# =====================================================================

script:

  # -------------------------------------------------------------------
  # script.schedule_set_zone_comfort
  # Nastaví danou climate entitu na cílovou teplotu zóny:
  # - režim Auto / Boost → poslední KOMFORT z helperu zóny
  # - režim Eco          → globální ECO teplota (input_number.eco_temp_default)
  # - režim Off          → skript nezasahuje (guard přes topny_rezim)
  # -------------------------------------------------------------------
  schedule_set_zone_comfort:
    alias: "Schedule: set zone comfort"
    mode: queued
    fields:
      climate_entity:
        description: "Target climate.* entita hlavice"
      last_comfort_helper:
        description: "Helper (input_number.*_last_comfort) s poslední komfortní teplotou zóny"
    sequence:
      - variables:
          mode: "{{ states('input_select.topny_rezim') }}"
          base: "{{ states(last_comfort_helper) | float(21) }}"           # fallback komfort
          eco_temp: "{{ states('input_number.eco_temp_default') | float(15) }}"  # globální ECO
          target: >-
            {% if mode == 'Eco' %}
              {{ eco_temp | round(1) }}
            {% else %}
              {{ base | round(1) }}
            {% endif %}

      # Režim Off → nerozjížděj nic
      - condition: template
        value_template: "{{ mode != 'Off' }}"

      # Přepnout na heat jen pokud už není, ať zbytečně nehýbeme režimem
      - choose:
          - conditions:
              - condition: template
                value_template: "{{ states(climate_entity) | lower != 'heat' }}"
            sequence:
              - action: climate.set_hvac_mode
                data:
                  entity_id: "{{ climate_entity }}"
                  hvac_mode: heat

      # Guard: zapisuj jen pokud se teplota reálně liší (±0.1 °C)
      - condition: template
        value_template: >-
          {{ (state_attr(climate_entity, 'temperature') | float(0)
              - (target | float(0))) | abs > 0.1 }}

      # Nastavení cílové (komfortní / ECO) teploty
      - action: climate.set_temperature
        data:
          entity_id: "{{ climate_entity }}"
          temperature: "{{ target }}"

      # Logbook – jen při reálné změně
      - action: logbook.log
        data:
          name: "{{ climate_entity.split('.')[-1] | replace('_',' ') | title }}"
          message: "Rozvrh – nastavena cílová teplota: {{ target }} °C (režim: {{ mode }})"
          entity_id: "{{ climate_entity }}"

  # -------------------------------------------------------------------
  # script.schedule_set_zone_setback
  # Nastaví danou climate entitu na útlumovou teplotu (setback)
  # -------------------------------------------------------------------
  schedule_set_zone_setback:
    alias: "Schedule: set zone setback"
    mode: queued
    fields:
      climate_entity:
        description: "Target climate.* entita hlavice"
    sequence:
      - variables:
          setback: "{{ states('sensor.heating_setback_temp_c') | float(17) }}"

      # Některé hlavice ignorují set_temperature v režimu off → hlídej heat
      - choose:
          - conditions:
              - condition: template
                value_template: "{{ states(climate_entity) | lower != 'heat' }}"
            sequence:
              - action: climate.set_hvac_mode
                data:
                  entity_id: "{{ climate_entity }}"
                  hvac_mode: heat

      # Guard: zapisuj jen pokud se teplota reálně liší (±0.1 °C)
      - condition: template
        value_template: >-
          {{ (state_attr(climate_entity, 'temperature') | float(0)
              - (setback | float(0))) | abs > 0.1 }}

      # Nastavení útlumové teploty
      - action: climate.set_temperature
        data:
          entity_id: "{{ climate_entity }}"
          temperature: "{{ setback }}"

      # Logbook – jen při reálné změně
      - action: logbook.log
        data:
          name: "{{ climate_entity.split('.')[-1] | replace('_',' ') | title }}"
          message: "Rozvrh – nastavena útlumová teplota: {{ setback }} °C"
          entity_id: "{{ climate_entity }}"