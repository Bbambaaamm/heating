# YAML 2024.10
# =====================================================================
# 🧠 SOUBOR: /config/heating/schedule/preferences/zone_michal_capture_comfort.yaml
# =====================================================================
# 🧲 Přízemí – Michal: zachytávání poslední KOMFORTNÍ teploty
# =====================================================================

automation:
  - alias: "Zachytni poslední komfort – Přízemí Michal"
    id: prizemi_michal_capture_last_comfort
    mode: restart
    initial_state: true

    triggers:
      - trigger: state
        entity_id: climate.prizemi_michal
        attribute: temperature

    variables:
      new_set: "{{ state_attr('climate.prizemi_michal', 'temperature') | float(0) }}"
      eco: "{{ states('input_number.eco_temp_default') | float(15) }}"

    conditions:
      - condition: template
        value_template: "{{ new_set >= (eco + 0.2) }}"
      - condition: template
        value_template: >
          {{ (states('input_number.prizemi_michal_last_comfort') | float(0) - new_set) | abs > 0.1 }}
      - condition: state
        entity_id: binary_sensor.kotel_boost_active
        state: "off"
        
    actions:
      - action: input_number.set_value
        target:
          entity_id: input_number.prizemi_michal_last_comfort
        data:
          value: "{{ new_set }}"

      - action: logbook.log
        data:
          name: "Přízemí – Michal"
          message: "Uložena komfortní teplota: {{ new_set }} °C"
          entity_id: climate.prizemi_michal
