# YAML 2024.10
# =====================================================================
# 🧠 SOUBOR: /config/heating/schedule/preferences/zone_2p_mama_capture_comfort.yaml
# =====================================================================
# 🧲 2. patro – Máma: zachytávání poslední KOMFORTNÍ teploty
# =====================================================================

automation:
  - alias: "Zachytni poslední komfort – 2. patro Máma"
    id: capture_last_comfort_2p_mama
    mode: restart
    initial_state: true

    triggers:
      - trigger: state
        entity_id: climate.2p_mama
        attribute: temperature

    variables:
      new_set: "{{ state_attr('climate.2p_mama', 'temperature') | float(0) }}"
      eco: "{{ states('input_number.eco_temp_default') | float(15) }}"

    conditions:
      # Ukládat jen komfort (nad ECO + 0.2 °C)
      - condition: template
        value_template: "{{ new_set >= (eco + 0.2) }}"
      # Ukládat jen při skutečné změně (> 0.1 °C)
      - condition: template
        value_template: >
          {{ (states('input_number.2p_mama_last_comfort') | float(0) - new_set) | abs > 0.1 }}
      - condition: state
        entity_id: binary_sensor.kotel_boost_active
        state: "off"

    actions:
      - action: input_number.set_value
        target:
          entity_id: input_number.2p_mama_last_comfort
        data:
          value: "{{ new_set }}"

      - action: logbook.log
        data:
          name: "2. patro – Máma"
          message: "Uložena komfortní teplota: {{ new_set }} °C"
          entity_id: climate.2p_mama
